{
  "name": "tweeter-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run compile && npm run configure_dist && npm run sam",
    "compile": "npm run clean && tsc",
    "clean": "rimraf dist",
    "configure_dist": "cd dist && npm init -y && npm install bcryptjs",
    "sam": "sam build && sam deploy --guided"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.940.0",
    "@aws-sdk/client-s3": "^3.943.0",
    "@aws-sdk/client-sqs": "^3.948.0",
    "@aws-sdk/lib-dynamodb": "^3.940.0",
    "@aws-sdk/util-dynamodb": "^3.943.0",
    "bcryptjs": "^3.0.3",
    "date-fns": "^3.3.1",
    "tweeter-shared": "file:../tweeter-shared",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/aws-sdk": "^0.0.42"
  }
}
